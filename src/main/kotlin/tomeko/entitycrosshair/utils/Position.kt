package tomeko.entitycrosshair.utils

//? if forge {
/*data class Position(val x: Int, val y: Int)

fun positionToIndex(x: Int, y: Int): Int = x + y * Constants.MAX_CANVAS_SIZE

fun indexToPosition(index: Int): Position = Position(index % Constants.MAX_CANVAS_SIZE, index / Constants.MAX_CANVAS_SIZE)
*///?}